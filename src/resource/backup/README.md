# 封存作品品質隔離

此目錄的 35 本舊作品目前標記為 `visibility: "quarantined"`，不得進入讀者書庫、reader manifest、GitHub Pages 產物或平台 catalog。

2026-07-04 全庫抽查確認，第 1、3 章存在跨書共用敘事骨架；即使移除完全相同的長句，段落順序與近似句法仍高度重合。這是整章重建問題，不可用批次同義改寫、刪句或替換人名冒充修復。

解除單本隔離前，必須逐章完成：

- 以該書人物、設定與既有事件重新建立場景順序，不能沿用舊模板句法。
- 覆讀三章的因果、人物狀態、物件來路與章末承接。
- 通過跨書完整句、完整段與 24 字長片段近似掃描。
- 通過禁用後設語、可讀節奏、具體回報與最後三行鉤子檢查。
- 在 `src/resource/manifest.json` 移除該書的 `visibility: "quarantined"` 後，重新執行測試、建置與部署產物驗證。

隔離中的文字只作重建素材與歷史紀錄，不是可發布成品。
