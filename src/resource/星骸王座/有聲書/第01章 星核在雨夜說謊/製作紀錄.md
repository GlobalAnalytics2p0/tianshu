# 《星骸王座》第01章有聲書製作紀錄

## 來源

- 正文章節：`src/resource/星骸王座/第01章 星核在雨夜說謊.txt`
- 朗讀開場：加入書名與章名，正文仍以原 `.txt` 為唯一 canonical 來源。

## 2026-06-07 本機流程驗證版

- 輸出檔案：`星骸王座-第01章-星核在雨夜說謊-Eddy.zh-TW.m4a`
- 語音：`Eddy (Chinese (Taiwan))`
- 語速：`160`
- 格式：AAC `.m4a`、mono、44.1kHz、約 124kbps
- 長度：約 16 分 56 秒
- 大小：約 15MB
- 工具：macOS `say` 產生 AIFF 中間檔，再以 `ffmpeg` 轉為 `.m4a`
- 品質判定：未達正式上線標準，只能作為管線驗證、章節長度估算與播放流程測試。

## 聲音判斷

這一版先採用年輕、偏冷、低情緒起伏的台灣中文男聲，對應沈曜被邊軍追殺、身體失血、星核冷聲介入的壓力感。朗讀節奏不做過度戲劇化，讓黑棺、星核與雨夜追兵保留懸疑。

## 已知限制

- macOS `say` 可作為本機第一版，但情緒細節、停頓與角色差異仍不如專業台灣華語 TTS 或真人錄音。
- 若後續接入雲端 TTS，優先測試更自然的台灣華語男聲，並保留冷調、克制、微疲憊的敘事方向。
- 音訊成品屬大型衍生檔，依目前規則保留在本機並由 `.gitignore` 忽略，不納入 Git commit。

## 正式版重產計畫

目標改用高品質台灣華語 TTS。若使用 OpenAI speech generation，先測 `cedar` 聲線與 `0.95` 語速，並透過 `scripts/generate-openai-audiobook.mjs` 產生 `OpenAI-cedar.zh-TW.m4a`。重產前需要設定 `OPENAI_API_KEY`，否則無法用雲端模型產音。

```bash
OPENAI_API_KEY=... node scripts/generate-openai-audiobook.mjs \
  --novel '星骸王座' \
  --chapter-title '第01章 星核在雨夜說謊' \
  --chapter-file 'src/resource/星骸王座/第01章 星核在雨夜說謊.txt' \
  --voice cedar \
  --speed 0.95 \
  --output 'src/resource/星骸王座/有聲書/第01章 星核在雨夜說謊/星骸王座-第01章-星核在雨夜說謊-OpenAI-cedar.zh-TW.m4a'
```

## 2026-06-07 Edge Neural TTS 候選版

- 輸出檔案：`星骸王座-第01章-星核在雨夜說謊-Edge-YunJhe.zh-TW.m4a`
- 語音：`zh-TW-YunJheNeural`
- 語速：`-6%`
- 音高：`-2Hz`
- 格式：AAC `.m4a`、mono、44.1kHz、約 116kbps
- 長度：約 16 分 51 秒
- 大小：約 14MB
- 工具：`edge-tts` 產生 MP3 中間檔，再以 `ffmpeg` 轉為 `.m4a`
- 品質判定：作為比 macOS `say` 更自然的台灣華語男聲候選版，可供人工抽聽評估是否上線。

## OpenAI API 嘗試紀錄

- 已建立一支 restricted API key，只開 `Text-to-speech (/v1/audio/speech)` 的 `Request` 權限。
- 使用 `gpt-4o-mini-tts`、`cedar`、`speed 0.95` 嘗試產生第 1 段時，OpenAI API 回傳 `insufficient_quota`。
- 因帳號目前沒有可用 quota / billing，OpenAI 版 `.m4a` 未產生。
- API key 未寫入 repo 或本機檔案；若之後不用，建議在 OpenAI Platform 刪除該 key。
